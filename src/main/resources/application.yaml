server:
  port: 8080

spring:
  application:
    name: flux-limiter
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
      lettuce:
        pool:
          enabled: false
        shutdown-timeout: 100ms